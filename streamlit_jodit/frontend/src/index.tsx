import React from "react"
import ReactDOM from "react-dom"
import StreamlitJodit from "./StreamlitJodit"

ReactDOM.render(
  <React.StrictMode>
    <StreamlitJodit />
  </React.StrictMode>,
  document.getElementById("root")
)
